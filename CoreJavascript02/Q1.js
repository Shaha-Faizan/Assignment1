/* Ceate an arrow function called squae that takes a numbe as an argument and retuns its squae. Use
the arrow function to calculate the squae of a given numbe and display the result*/

const square = (num)=>num*num;
const num = 10;
const squareNumber = square(num)
console.log("Square:", squareNumber);
